---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: unbound-service-reader
  namespace: networking
rules:
  - apiGroups: [""]
    resources: ["services"]
    verbs: ["get", "list"]
